# Ember realtime trading

Standalone realtime market-grid benchmark for the TanStack Ember Table adapter.
It mirrors the same feed, controls, table features, diagnostics, and row
rendering modes used by the other framework examples.

```bash
pnpm dev
pnpm test:types
pnpm build
```

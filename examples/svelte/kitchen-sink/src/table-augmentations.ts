// Table type augmentations have been moved to the features object in App.svelte.
// Custom fns (fuzzy filter/sort) are registered in the filterFns/sortFns slots
// alongside the individually imported built-in fns the example uses,
// and filterMeta is part of stockFeatures.
export {}
